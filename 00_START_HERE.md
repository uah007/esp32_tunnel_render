# 📦 Адаптированный проект для Render.com

## ✅ Что готово

Ваш проект полностью адаптирован для **бесплатного** размещения на Render.com!

---

## 📁 Структура файлов

### 🌐 Для деплоя на Render.com (`/render_deployment/`)

```
render_deployment/
├── server_websocket.js      ← WebSocket сервер (главный файл)
├── package.json             ← Node.js зависимости
├── .gitignore              ← Что не загружать в Git
├── public/
│   └── index.html          ← Веб-интерфейс для браузера
├── README.md               ← Описание проекта
├── QUICK_START.md          ← Быстрый старт (5 минут)
├── RENDER_DEPLOY.md        ← Полная инструкция деплоя
└── MIGRATION_GUIDE.md      ← Сравнение старой/новой версии
```

### 🔧 Для ESP32 (`/esp32_code/`)

```
esp32_code/
└── esp32_websocket.ino     ← Код для ESP32 с WebSocket
```

---

## 🚀 Быстрый старт

### 1. Деплой на Render.com (3 минуты)

1. **Создайте GitHub репозиторий:**
   - Загрузите все файлы из папки `render_deployment/`

2. **Создайте Web Service на Render.com:**
   - Зайдите на [render.com](https://render.com)
   - New → Web Service
   - Подключите GitHub репозиторий
   - Build Command: `npm install`
   - Start Command: `npm start`
   - Plan: **Free**
   - Deploy!

3. **Настройте UptimeRobot:**
   - [uptimerobot.com](https://uptimerobot.com)
   - Add Monitor → HTTP(s)
   - URL: `https://ваш-сервис.onrender.com/health`
   - Interval: 5 minutes

✅ **Сервер готов!** Получите URL: `https://ваш-сервис.onrender.com`

---

### 2. ESP32 Setup (5 минут)

1. **Установите библиотеки в Arduino IDE:**
   - Tools → Manage Libraries
   - Установите:
     - `PubSubClient` by Nick O'Leary
     - `ArduinoJson` by Benoit Blanchon
     - `arduinoWebSockets` by Markus Sattler

2. **Загрузите код:**
   - Откройте `esp32_websocket.ino`
   - Измените WiFi настройки:
     ```cpp
     const char* ssid = "ВАШ_WIFI";
     const char* password = "ВАШ_ПАРОЛЬ";
     ```
   - Загрузите на ESP32

3. **Отправьте MQTT команду:**
   ```
   ваш-сервис.onrender.com,443,192.168.1.100,80
   ```
   Где:
   - `ваш-сервис.onrender.com` - URL с Render.com
   - `443` - порт (всегда 443 для Render.com)
   - `192.168.1.100` - IP целевого устройства
   - `80` - порт целевого устройства

---

### 3. Использование

**Вариант A: Через браузер (проще всего)**

Откройте в браузере:
```
https://ваш-сервис.onrender.com/?tunnel=любой_id
```

**Вариант B: Python GUI (требует адаптации)**

Ваш текущий Python GUI работает с локальным сервером.
Для работы с Render.com нужно либо:
- Использовать веб-интерфейс (вариант A)
- Адаптировать GUI для WebSocket
- Оставить локальный сервер как прокси к облаку

---

## 📖 Документация

Откройте эти файлы для подробной информации:

- **`QUICK_START.md`** - краткое руководство на 5 минут
- **`RENDER_DEPLOY.md`** - детальная инструкция деплоя
- **`MIGRATION_GUIDE.md`** - сравнение TCP vs WebSocket
- **`README.md`** - полное описание проекта

---

## 🔧 Формат MQTT команды

```
сервер,порт,целевой_IP,целевой_порт
```

**Примеры:**

Render.com (всегда HTTPS/WSS на порту 443):
```
my-tunnel.onrender.com,443,192.168.1.254,80
```

Локальное тестирование:
```
localhost,8080,192.168.1.254,80
```

---

## 💰 Стоимость: $0 навсегда!

| Компонент | Тариф | Цена |
|-----------|-------|------|
| Render.com | Free (750 часов) | $0 |
| UptimeRobot | Free | $0 |
| MQTT broker | Public | $0 |
| ESP32 | Одноразово | ~$3-5 |
| **Эксплуатация** | | **$0/месяц** |

---

## ✨ Ключевые изменения

### Что изменилось от оригинала:

1. **Сервер**: TCP → WebSocket (совместимость с Render.com)
2. **ESP32**: WiFiClient → WebSocketsClient
3. **Протокол**: Чистый TCP → WebSocket (WSS)
4. **Порт**: 9000 → 443 (HTTPS)
5. **SSL/TLS**: Ручная настройка → Автоматически

### Что осталось прежним:

1. **Логика работы**: Та же самая
2. **MQTT управление**: Без изменений
3. **Прозрачность**: Полный туннель TCP данных
4. **Python GUI**: Может остаться как есть (с локальным прокси)

---

## 🎯 Следующие шаги

### Минимум (для работы):

1. ✅ Деплой на Render.com
2. ✅ Настройка UptimeRobot
3. ✅ Загрузка кода на ESP32
4. ✅ Тест через браузер

### Опционально (улучшения):

- [ ] Адаптировать Python GUI для прямого подключения
- [ ] Добавить аутентификацию (пароли)
- [ ] Улучшить веб-интерфейс
- [ ] Добавить свой домен
- [ ] Настроить мониторинг

---

## 🆘 Поддержка

### Проблемы с деплоем?

1. Проверьте логи Render.com: Dashboard → Logs
2. Убедитесь что `package.json` и `server_websocket.js` в корне репозитория
3. Build Command: `npm install`
4. Start Command: `npm start`

### ESP32 не подключается?

1. Проверьте Serial Monitor
2. Убедитесь что библиотека `arduinoWebSockets` установлена
3. Используйте порт **443** для Render.com
4. Проверьте формат MQTT команды

### Сервер засыпает?

1. Настройте UptimeRobot
2. URL: `https://ваш-сервис.onrender.com/health`
3. Interval: 5 minutes

---

## 📊 Диаграмма работы

```
1. Python GUI отправляет MQTT команду
        ↓
2. ESP32 получает команду
        ↓
3. ESP32 подключается к Render.com (WSS)
        ↓
4. ESP32 подключается к целевому устройству (TCP)
        ↓
5. Браузер открывает https://render-url/?tunnel=id
        ↓
6. Браузер ↔ Render.com ↔ ESP32 ↔ Целевое устройство
```

---

## 🎉 Готово!

Ваш проект готов к деплою на Render.com!

**Начните с файла `QUICK_START.md` для быстрого старта.**

Удачи! 🚀
