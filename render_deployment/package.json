{
  "name": "esp32-tunnel-server",
  "version": "1.0.0",
  "description": "HTTP Long Polling tunnel server for ESP32 NAT traversal",
  "main": "server_http_polling.js",
  "scripts": {
    "start": "node server_http_polling.js"
  },
  "dependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
