# ESP32 NAT Tunnel - WebSocket версия для Render.com

## 🎯 Что изменилось

Адаптация проекта для бесплатного хостинга на Render.com:
- ✅ Сервер переписан с TCP на **WebSocket**
- ✅ ESP32 код адаптирован под **WebSocket клиент**
- ✅ Добавлен health check endpoint для Render.com
- ✅ Python GUI работает без изменений (или с минимальными)

## 📦 Файлы проекта

### Для Render.com (сервер):
- `server_websocket.js` - WebSocket сервер
- `package.json` - зависимости Node.js

### Для ESP32:
- `esp32_websocket.ino` - код для ESP32 с WebSocket
- Требуется библиотека: **arduinoWebSockets** by Markus Sattler

### Для Python GUI:
- Код остается прежним или требует минимальных изменений

---

## 🚀 Деплой на Render.com

### Шаг 1: Подготовка репозитория

1. Создайте новый GitHub репозиторий
2. Загрузите туда файлы:
   ```
   repository/
   ├── server_websocket.js
   ├── package.json
   └── README.md
   ```

### Шаг 2: Создание веб-сервиса на Render.com

1. Зайдите на [render.com](https://render.com) и зарегистрируйтесь
2. Нажмите **"New"** → **"Web Service"**
3. Подключите ваш GitHub репозиторий
4. Настройки:
   - **Name**: `esp32-tunnel` (или любое имя)
   - **Environment**: `Node`
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
   - **Plan**: **Free** ✅

5. Нажмите **"Create Web Service"**

### Шаг 3: Получите URL вашего сервера

После деплоя Render.com даст вам URL типа:
```
https://esp32-tunnel.onrender.com
```

**Важно**: URL будет с HTTPS (порт 443), WebSocket будет работать как WSS (Secure WebSocket)

---

## 🔧 Настройка UptimeRobot (чтобы сервис не засыпал)

Render.com бесплатный план засыпает через 15 минут бездействия.

### Шаг 1: Создайте аккаунт на UptimeRobot

1. Зайдите на [uptimerobot.com](https://uptimerobot.com)
2. Создайте бесплатный аккаунт

### Шаг 2: Добавьте монитор

1. Нажмите **"Add New Monitor"**
2. Настройки:
   - **Monitor Type**: HTTP(s)
   - **Friendly Name**: ESP32 Tunnel Keep-Alive
   - **URL**: `https://ваш-сервер.onrender.com/health`
   - **Monitoring Interval**: 5 minutes

3. Сохраните

Теперь сервер будет "пинговаться" каждые 5 минут и не будет засыпать!

---

## 📱 Настройка ESP32

### Установка библиотек

В Arduino IDE установите:
1. **PubSubClient** by Nick O'Leary
2. **ArduinoJson** by Benoit Blanchon
3. **arduinoWebSockets** by Markus Sattler (важно!)

### Настройка кода

В файле `esp32_websocket.ino` измените:

```cpp
// WiFi
const char* ssid = "ВАШ_WIFI";
const char* password = "ВАШ_ПАРОЛЬ";

// MQTT (если используете другой брокер/топик)
const char* mqtt_server = "broker.emqx.io";
const char* mqtt_topic = "ВАШ_ТОПИК";
```

### Формат MQTT сообщения

Отправьте в MQTT топик строку в формате:
```
ваш-сервер.onrender.com,443,192.168.1.100,80
```

Где:
- `ваш-сервер.onrender.com` - домен вашего Render.com сервиса
- `443` - порт (всегда 443 для Render.com)
- `192.168.1.100` - IP целевого устройства в локальной сети ESP32
- `80` - порт целевого устройства

---

## 💻 Python GUI

### Вариант 1: Без изменений (рекомендуется)

Python GUI запускает **локальный** Node.js сервер, который работает как прокси:

1. Python отправляет MQTT команду
2. ESP32 подключается к **облачному** серверу на Render.com
3. Python подключается к **локальному** серверу на localhost:9000
4. Локальный сервер форвардит все на облачный

**Для этого нужен отдельный локальный сервер-прокси** (опционально, можно обойтись)

### Вариант 2: Прямое подключение к Render.com (проще)

Можно упростить: Python GUI напрямую открывает браузер с URL:
```
https://ваш-сервер.onrender.com/?tunnel=TUNNEL_ID
```

Для этого нужно добавить простую HTML страницу на сервер, которая будет делать WebSocket подключение и отображать content.

---

## 🧪 Тестирование

### 1. Проверка сервера

Откройте в браузере:
```
https://ваш-сервер.onrender.com/health
```

Должно показать: `OK`

### 2. Проверка WebSocket

Используйте любой WebSocket тестер или `wscat`:
```bash
npm install -g wscat
wscat -c wss://ваш-сервер.onrender.com
```

### 3. Полный тест туннеля

1. Загрузите код на ESP32
2. Убедитесь что ESP32 подключился к WiFi и MQTT
3. Отправьте MQTT команду с адресом Render.com сервера
4. ESP32 должен подключиться к WebSocket серверу
5. Откройте браузер (или используйте Python GUI)
6. Проверьте что можете обращаться к целевому устройству

---

## 📊 Архитектура

```
┌─────────────┐         MQTT          ┌──────────┐
│ Python GUI  │────────────────────────▶│  ESP32   │
└─────────────┘                        └──────────┘
      │                                      │
      │ HTTP                            WebSocket (WSS)
      │                                      │
      ▼                                      ▼
┌─────────────────────────────────────────────────┐
│     Render.com (WebSocket Server)               │
│     https://ваш-сервер.onrender.com             │
└─────────────────────────────────────────────────┘
                                                │
                                                │
                                                ▼
                                        ┌───────────────┐
                                        │ Целевое       │
                                        │ устройство    │
                                        │ 192.168.1.X   │
                                        └───────────────┘
```

---

## 🔍 Troubleshooting

### ESP32 не подключается к WebSocket

- Проверьте что используете **порт 443**
- Убедитесь что домен правильный (без `https://`, только домен)
- Проверьте что библиотека arduinoWebSockets установлена

### Render.com сервис засыпает

- Настройте UptimeRobot (см. выше)
- Проверьте что пинг идет на `/health` endpoint

### Целевое устройство не отвечает

- Проверьте что ESP32 может достучаться до целевого IP
- Убедитесь что целевое устройство работает
- Проверьте firewall на целевом устройстве

---

## 💰 Стоимость

**Render.com Free tier:**
- ✅ Бесплатно навсегда
- ✅ 750 часов работы в месяц (31+ день постоянной работы)
- ✅ Автоматический SSL (HTTPS/WSS)
- ⚠️ Засыпает через 15 минут бездействия (решается UptimeRobot)
- ⚠️ Холодный старт ~30 секунд

**UptimeRobot:**
- ✅ Бесплатно навсегда
- ✅ До 50 мониторов
- ✅ Проверка каждые 5 минут

**Итого: ПОЛНОСТЬЮ БЕСПЛАТНО! 🎉**

---

## 📝 Примечания

- Render.com требует HTTPS, поэтому WebSocket работает как **WSS** (Secure WebSocket)
- ESP32 использует `webSocket.beginSSL()` для подключения
- Трафик автоматически шифруется через TLS
- Для локальной разработки можно использовать порт 8080 и обычный WS

---

## 🆘 Поддержка

Если возникли проблемы:
1. Проверьте логи на Render.com (Dashboard → Logs)
2. Проверьте Serial Monitor ESP32
3. Проверьте что UptimeRobot активен

Готово! Ваш туннель работает на бесплатном хостинге! 🚀
